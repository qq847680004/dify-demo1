# 询价字段抽取

从非结构化**询价文本**中抽出固定业务字段，供 Dify Workflow 产出严格 JSON。本 glossary 只定义领域用语，不含 DSL 实现细节。

## Language

**询价文本**：
客户侧发来的询盘/询价原文（可能尚未报价）。工作流的唯一输入形态。
_Avoid_: 报价单（作为主称呼）、询价单文件（除非用户明确上传文件）

**客户名称**：
询价文本中最先出现的公司名（本项目运营方为物流公司，接受「最先出现」策略，不因货代/承运商误伤而改规则）。
_Avoid_: 买方（未单独建模时）、收货人、发货人（未单独字段时不要偷换）

**物流类型**：
仅 `海运` 或 `空运`；一文同时出现时取**先出现**的那个；完全无法判断则为空。
_Avoid_: 陆运、快递（当前契约不收录）；多值数组

**紧急程度**：
仅 `紧急` 或 `普通`；未提及则为空字符串。同文冲突时：任一「紧急/加急/ASAP/急单」等信号则取 `紧急`（即使也有「不急」）。
_Avoid_: 是否紧急、是/否；把未提及默认成 `普通`

**起运港 / 目的港**：
各拆成英文或代码字段与中文字段；原文出现什么填什么，禁止臆造未出现的代码或译名。
_Avoid_: 港口（未区分起运/目的时的笼统说法）

**货品规格**：
重量、体积等规格原文（如约1,200KG，6CBM），保持原文表述。
_Avoid_: 单独的重量字段、体积字段（当前未拆）

**报价金额**：
浮点数值；未出现时为 `0`。一文多金额时取**第一个**；`0` 且货币单位为空表示未抽到报价。
_Avoid_: 把币种写进金额字符串；多行报价数组

**货币单位**：
`¥`、`$`、`€` 等符号；与所选金额对齐；代码如 USD/CNY 应归一为符号；缺失为空字符串。
_Avoid_: USD/CNY 原样留在终态（应归一）

**报价条款**：
贸易条款词（如 EXW、CIF）；一文多条款时只取**第一个**条款词；不把原文擅自改成另一套标准缩写。
_Avoid_: 把多个互斥条款拼进同一字符串；Incoterms（除非强调标准集合）

**服务范围**：
询价中要求提供的服务项原文；多项用两侧带空格的 ` / ` 连接。
_Avoid_: 服务数组（当前契约为单字符串）；无空格 `/`、顿号 `、`

**抽取结果 JSON**：
工作流对外验收对象；下游以 API 的 `result_json` 为准，不要求 Web 端并排展示全部标量。提取失败时仍输出固定 13 键：字符串为 `""`，`quote_amount` 为 `0`（`urgency_level` 亦为 `""`）。
_Avoid_: 把标量字段当主契约
