# 询价字段抽取

从非结构化**询价文本**或**询价图片**中抽出固定业务字段，供 Dify Workflow 产出严格 JSON。本 glossary 只定义领域用语，不含 DSL 实现细节。

## Language

**询价文本**：
客户侧发来的询盘/询价原文（可能尚未报价）。文本抽取工作流的输入形态。
_Avoid_: 报价单（作为主称呼）；把询价图片叫成询价文本

**询价图片**：
客户侧发来的询盘截图、扫描件或拍照。图片抽取工作流的主输入；单张、仅图片。
_Avoid_: 询价文本；PDF/多图（当前未做）

**转写文本**：
从询价图片按阅读顺序得到的可见文字，作为图片抽取下游的「原文」。只转写、不翻译、不补全。
_Avoid_: 把摘要、推测航线、翻译英文当成转写文本

**客户名称**：
询价文本中最先出现的公司名（本项目运营方为物流公司，接受「最先出现」策略，不因货代/承运商误伤而改规则）。输入 `fuzzy_match_customer=1` 时：无公司名则取客户人名，再无则取邮箱 `@` 前用户名；默认 `0` 仍只取公司名。
_Avoid_: 买方（未单独建模时）、收货人、发货人（未单独字段时不要偷换）；默认模式下用人名或邮箱名冒充公司名

**运输方式**：
仅 `空运`、`海运`、`铁运`、`汽运` 或 `多式联运`；一文同时出现时取**先出现**的那个；完全无法判断则为空。同义归一：铁路/rail→铁运；公路/陆运/卡车→汽运；FCL/LCL/整箱/拼箱→海运。
_Avoid_: 快递（五档外，单独出现则空）；海运 FCL/海运 LCL（不再拆箱型）；物流类型（旧名）；多值数组

**紧急程度**：
仅 `紧急` 或 `普通`；未提及则为空字符串。同文冲突时：任一「紧急/加急/ASAP/急单」等信号则取 `紧急`（即使也有「不急」）。
_Avoid_: 是否紧急、是/否；把未提及默认成 `普通`

**起运地 / 目的地**（JSON 键仍为 `origin_port_*` / `dest_port_*`）：
各拆成英文或代码字段与中文字段。英文/代码侧为英文名或国际地点代码（UN/LOCODE、IATA、铁路电报码等）；中文侧为城市/地区地名（如 `宁波`、`洛杉矶`），**不带「港」字**；空运/海运/铁运/汽运/多式联运同样适用。原文出现什么填什么，禁止臆造未出现的代码或译名。
_Avoid_: 中文侧输出「宁波港」等带「港」后缀；未区分起运/目的时的笼统说法；把 UN/LOCODE 当公司名或货号丢掉

**货品规格**：
重量、体积等规格原文（如约1,200KG，6CBM），保持原文表述。
_Avoid_: 单独的重量字段、体积字段（当前未拆）

**报价条款**：
贸易条款词（如 EXW、CIF）；一文多条款时只取**第一个**条款词；不把原文擅自改成另一套标准缩写。
_Avoid_: 把多个互斥条款拼进同一字符串；Incoterms（除非强调标准集合）

**服务范围**：
询价中要求提供的服务项原文；多项用两侧带空格的 ` / ` 连接。
_Avoid_: 服务数组（当前契约为单字符串）；无空格 `/`、顿号 `、`

**抽取结果 JSON**：
工作流对外验收对象；下游以 API 的 `result_json` 为准，不要求 Web 端并排展示全部标量。提取失败时仍输出固定 11 键：字符串为 `""`（`urgency_level` 亦为 `""`）。
_Avoid_: 把标量字段当主契约
